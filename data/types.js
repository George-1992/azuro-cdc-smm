const allTypes = {
    sources: {
        options: [
            // 'pending',
            'processing', 'processed', 'failed'
        ],
    }
}

export const webhookTypes = [
    'sources',
    'avatars',
]

export default allTypes;